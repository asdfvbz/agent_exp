# 统一换行符。
#
# **这在本项目不是洁癖,是正确性问题:** 仓库里有 shell 脚本
# (plugins/exp/bin/exp),Windows 上 checkout 成 CRLF 会让 shebang
# 变成 `#!/usr/bin/env python3\r`,在 Linux/macOS 上直接执行失败。
#
# 经验文件也一样:它们是用户的数据,跨平台 checkout 后内容必须一致,
# 否则 diff 会满屏都是换行符差异。
* text=auto eol=lf

# 例外:Windows 批处理必须是 CRLF
*.cmd text eol=crlf
*.bat text eol=crlf

# 二进制,别碰
*.png binary
*.jpg binary
*.ico binary
