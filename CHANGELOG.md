# 变更记录

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/),
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

### 新增:任务前投递 —— "按需加载"的那个"按需"

在此之前,会话里**没有任何一条通道能在动作之前把经验送到模型眼前**:

| 通道 | 时机 | 送什么 |
|---|---|---|
| `SessionStart` | 会话开始 | 全量索引(标题 + 触发词),**与当前任务无关** |
| `PostToolUseFailure` | 失败**之后** | fix(已经晚了) |
| `exp query` | 靠模型自觉 | —— |

于是**会话是失效边界**:每条经验的 `fix` 只在你已经踩过一次之后才送达。

- **新增 `UserPromptSubmit` hook**:拿用户的 prompt 检索,**命中条目的
  正文**(何时适用 / 根因 / 做法)随 prompt 一起进上下文。
- **为什么不是 `PreToolUse`** —— 它看着更合适(知道要跑什么),但它的
  `additionalContext` 是**和工具结果同一次**送达的,模型看到经验时命令
  已经跑完了。这不是实现选择,是 Messages API 的结构: `tool_result`
  必须紧跟 `tool_use`,**中间没有位置可插**。PreToolUse 能改参数、
  能拦,但**不能提前告知**。代价是 UserPromptSubmit 只看得到用户的
  自然语言,看不到 `tool_input` —— 看得见的意图远不如到得及的时机重要。
- **一次只推一条。** 实测:一条 prompt 命中多条时,真正相关的那条
  往往不是第一条,推两条就是"一条相关 + 一条不相关",而不相关的那条
  会一直留在会话历史里。推一条:错了只错一条。
- **同一会话不重复推。** `additionalContext` 会留在会话历史里,
  反复注入同一条不是提醒,是每轮都在烧的上下文。
- **记账是 `content` 级**,这让 `recurred` 有了更严格的含义:
  **任务开始前就把做法给过你了,你还是踩了。**

#### 阈值是量出来的,不是拍的

用真实会写的 trigger 和真实风格的 prompt 测(6 正例 / 3 反例):

```
阈值 1:  命中 5/6   误报 1/3
阈值 2:  命中 0/6   误报 0/3     ← 功能等于关闭
阈值 3:  命中 0/6   误报 0/3
```

**两类在 1 分处完全重叠**(A 类分布 `{0,1,1,1,1,1}`,B 类 `{0,0,1}`)。
也就是说二元组分数几乎只区分"同不同话题",不区分"相关不相关"。
**真正在做区分的是排序,不是阈值** —— 明显无关的 prompt("解释一下
这个函数""总结项目架构")拿到 0 被挡住,进来的由 `rank` 选最像的那条。
取 2 会让整个功能永不触发,那是死代码,比有点噪声更坏。

### 修复:中文二元组里的高频虚词在给相关度灌水

实测同一个 prompt 对两条经验的得分:

```
"我要设一个超时阈值"      与 "当你写一个数字阈值时"  → 2 分
"我要设一个重试次数阈值"   与 同一条                  → 也是 2 分
```

两次共有的都是**「一个」+「阈值」** —— "超时"和"重试次数"的差别被
虚词抹平了。**打分器在分辨两条相关经验时,靠的是噪声。**
加了中文/英文停用词表(只收真正哪里都出现的词,范围宁可小:
误删实词是静默漏检,而 `missed` 计数就是用来暴露漏检的)。

### 修复:插件从来没跑过(两个致命 bug)

这两条是装上之后才暴露的,**读代码读不出来,60 个单元测试也全绿**。

- **`plugin.json` 声明了 `hooks` 字段 → 插件加载失败。**
  `hooks/hooks.json` 是**自动发现**的标准位置,再声明一次会判重复:
  `Duplicate hooks file detected`,插件状态是 `✘ failed to load`,
  三个 hook 一个都不注册。后果是**任何用户装上之后,机制一次都不会
  触发**,而且没有报错通向用户 —— 除非主动跑 `claude plugin list`。
  删掉那个字段即可。
- **三个 hook 命令在 Windows 上全部 ParserError。**
  Claude Code 在 Windows 上用 **PowerShell** 执行 hook 命令,而命令串里
  `${CLAUDE_PLUGIN_ROOT}` 是 bash 语法(被改写成 `${env:...}` 后报错),
  `||` **不是 PowerShell 运算符**(旧版直接 InvalidEndOfLine)。
  两个错误叠在一起,表现同样是**三个 hook 全部静默不执行** ——
  hook 失败是非阻塞的,只在 transcript 里留一条 `hook_non_blocking_error`。
  命令包成 `bash -c '...'` 后,变量展开和 `||` 都交给 bash,三平台一致。

> **"测试必须打真实入口"这条教训,在插件这一层又重演了一次。**
> 为 `session_id` 那个 bug 写过一次,而插件**从没被 `install` 过**,
> 所以真实入口从来没被打过。单元测试测的是 `cmd_hook` 这个函数,
> 而它上面还压着一层"hook 到底有没有被注册、命令串跑不跑得起来"。

### 新增:raw 里的签名接回投递路径

修一个**投递侧**的大洞。签名在**失败发生的那一刻**就算出来了,也落进了
`raw/`,但它只被用来匹配**已经挂了签名的经验**。于是:

> raw 里躺着"这是同一种失败"的结构性确证,
> 投递却在用两个同样失效的启发式检索器找一条从没挂过它的经验。

后果是**第 2 次踩坑和第 1 次完全同形**:exit 0、什么都不送。系统手里
握着"你犯过这个错"的硬证据,却保持沉默。

- **库里没有对应签名时,同一签名当天撞到第 2 次就告诉模型。**
  带工具名和报错原文(签名里的 `<STR>` 归一化过,只给签名模型认不出
  对应哪条报错),并给出可执行的下一步。**只说一次** —— 第 3 次再报
  就是纯噪声。判据是**纯结构性**的(同一签名出现 N 次),不依赖
  trigger 措辞,也不需要库里先有任何东西 —— **冷启动期唯一能工作的信号**。
- **为什么不是语义兜底**:实测 `query(f"{tool} {err}", min_overlap=3)`
  在 6 个真实场景上命中 **0/6**。报错描述的是**症状**,trigger 描述的是
  **你正要做什么** —— 这两套词汇在设计上就不相交。签名之所以是
  唯一的确定性匹配,正是因为它绕开了这个矛盾。所以这里**不加新启发式**,
  只把 raw 里的结构性事实接回来。
- **`exp add --from-raw <签名>`**:从 raw 抄签名,替掉手抄那一步。
  与 `--signature` 的区别是**会校验该签名确实出现过** —— 抄错的签名
  是个永远不命中的**静默**坏钩子(失败时匹配不上,系统当这条经验不存在,
  而库里显示一切正常)。
- 报错文本为空时**不参与**提示:那样的签名是退化的(`Bash||`),
  所有"没有报错的失败"会塌缩到同一个签名上 —— 那不是"同一种失败"的
  证据,是"没有信息"。假阳性比不提示更坏。

### 修复:`session_id()` 读错了环境变量名

查的是 `CLAUDE_SESSION_ID`,而 Claude Code 实际设置的是
**`CLAUDE_CODE_SESSION_ID`**(2.1.132+)。后者不存在,永远读不到东西。
后果是所有**手工调用**(`exp query` / `exp show`)的注入都记在空会话名下
—— 而"模型到底主动查过没有"恰恰就是靠手工调用产生的信号,
等于把要量的那个量本身弄丢了。现在两个都查,权威名字优先。

### 测试

- 新增 `TestRepeatHint` / `TestFromRaw` / `TestPreActionDelivery`
  三个类,**18 个用例**。「库里没有对应经验时重复失败必须被送达」
  和「任务开始前把相关经验送到」这两条路径此前
  **在任何测试里都没有覆盖** —— 这正是它们藏住的原因。
- `Base.setUp` 清理环境变量时补上 `CLAUDE_CODE_SESSION_ID`。
  不清的话,在真实会话里跑测试时开发机的变量会漏进被测代码,
  让某些用例"碰巧通过" —— 这正是上一版漏掉 `session_id` bug 的机制。
- 用例数 60 → **78**,两条 YAML 路径与三个平台一致。

### 修复:归因闭环之前是断的

三个问题叠在一起,导致 `recurred` **恒为 0**,而且症状完全静默。

- **`session_id()` 在环境变量缺失时回退到 `pid{getppid()}`,而 hook 每次
  调用的 `ppid` 都不同**(命令中间隔着一层 shell)。于是注入和归因永远
  对不上号 → 所有签名精确命中的复发都被误记成 `missed`。现在以 hook
  payload 里的 `session_id` 为准,拿不到就显式返回空、跳过 recurrence 判定,
  绝不编一个 id 出来。
- **投递和归因用了两个不相交的检索器**:归因走 `by_signature`(精确),
  投递走 `query`(模糊)。于是一条挂了签名的经验可以被抓去归因,却因为
  二元组匹配不上 trigger 而**送不到模型手上**。现在两处都先骨架、后语义。
- **`post-failure` 把 fix 送给了模型,却没记这次注入** —— 模型真的读过做法,
  系统却当没送过。现在送达即记账。

### 新增:两个只用机器就能算出来的信号

- **`starved`(投递饿死)** —— 签名精确命中却从没送到模型手上。
  这不是经验的问题,是投递策略的问题;`gc` 会明说 **别动 trigger**,
  并在索引排序里把它提前 —— 否则"越饿死越靠后、越靠后越饿死"会自我锁定。
- **`indexed` / `excluded` + `starved_index`(索引饿死)** —— 把"进过索引
  但没人读"和"连索引都没进过"分开。两者在数据上都是"注入 0 次",
  但前者该删、后者该扩容量。上一版把它们一起报成死重,会让人删掉一批
  只是没排上队的经验。
  **判据用 `excluded`(渲染时当场记账的被挤掉次数),不用年龄** ——
  容量溢出是算术事实,今天超了今天就成立,不该等 30 天。
- **`exp gc` 新增容量告警**:库比索引大时立刻报,并说明这些经验的
  `missed` 永远算不出来 —— 这正是 README 里"最隐蔽的失效模式"的现场。

### 容量:从 40 条提到 100 条,零额外开销

- **真正约束成本的是字符预算,不是条数。** `INDEX_MAX_ITEMS` 曾是 40,
  而 4000 字符预算实测能装约 100 条 —— 一半预算白白浪费。
  现在条数只作防爆上限(120),有效容量由字符预算决定。
  实测 58 条经验从"40 条常驻 / 18 条饿死"变成全部常驻。
- **截断提示改成对模型可行动的。** 原来写"跑 `exp cluster` 看哪些该合并"——
  那是给维护者看的,而读它的是模型。现在告诉它库里共多少条、
  以及用 `exp query` 一样能检索到没列出的。**饿死的真正危害不是
  "没列出来",而是"模型不知道它存在"。**
- **容量告警报出真正生效的那条约束。** 卡在字符预算就说字符预算,
  卡在条数就说条数 —— 报错的限制器会把人引向错误的处置。

### 变更

- 归因拆成 `骨架 × 看没看过` 四个格子。**结构性信号不再产生 `missed`** ——
  签名命中是硬证据,那条 trigger 一个字都不用改。
- 索引排序键:`-recurred` → `injected > 0`(用过的优先占位),
  末级 `title` → `created` + `id`(稳定,新经验不会无故挤掉老的,
  而被挤掉的集合决定 `starved_index` 的认定,顺序不能是任意的)。
- `health()` 新增 `starved` / `starved_index` 两档;`gc` 的处置建议按类分开。
- 网页面板同步:新增两个 KPI、两个分组、两个字形(`◇` `○`,灰度下可区分),
  饿死类用蓝色而非红黄 —— 色系本身就在提示"别改经验"。

### 测试

- **归因用例改走真实入口**(`Base.hook()` 从 stdin 喂 `cmd_hook`),
  删掉了手抄一份归因逻辑的 `_fail()`。上一版测的是抄本,真正的 hook
  路径从没被覆盖 —— 这正是上面那个 `session_id` bug 藏住的原因。
- 用例数 45 → 55。

### 即将到来
- 卸载后 `.exp/` 的主动提示
- 索引超过容量时的自动分层(目前只是报告)

## [0.1.0] — 2026-09-19

首个可用版本。

### 核心机制

- **两层经验库**:项目级(`<项目>/.exp/`,跟 repo 走)+ 全局级(`~/.exp/`,跟人走)。
  检索时合并,全局降权。
- **机器强制捕获**:失败时由 `PostToolUseFailure` hook 记录,不依赖自觉。
- **注入记账 + 复发归因**:区分 `index`(只看到标题)和 `content`(看到做法)
  两级注入,据此自动算出两个信号:
  - `recurred` —— 看过做法还是犯,说明 fix 不可执行
  - `missed` —— 该命中却没命中,说明触发词写偏了
- **自动降级**:复发到阈值且已确认的经验,自动标记 `needs_rewrite`。
- **索引容量报告**:截断时明确告知,避免"经验静默漏注入"。

### 命令

`init` `hook` `query` `show` `list` `add` `feedback` `distill` `cluster`
`gc` `serve` `migrate` `status` `import` `pack` `stats`

### 工程

- **零依赖**:纯标准库;装了 PyYAML 用 PyYAML,没装走内置 YAML 子集。
  两条路径都在 CI 里真实覆盖。
- **schema 版本号**:每个经验文件带 `schema:` 字段,`exp migrate` 按版本升级
  (默认 dry-run)。
- **解析缓存**:按 `(mtime, size)` 失效。199 条时 hook 耗时从 280ms 降到 ~88ms。
- **跨进程文件锁**:计数读-改-写全程加锁,40 并发写入零丢失。
- **45 个回归测试** × 3 平台 × 2 Python 版本 × 有无 PyYAML。

### 已知边界

- 注入 ≠ 采纳。系统保证经验被记下并送到模型眼前,不保证它采纳 ——
  `recurred` 计数就是用来暴露这个落差的。
- 归因是**相对信号**,不是因果结论。
- 二元组检索匹配不到同义表达,`trigger` 措辞要贴近你实际会想到的词。
- 冷启动期(约前一周)基本无用,价值从第 3-4 周开始显现。
- 捕获层只覆盖"有工具报错"的坑。没有失败事件的坑(如"这段设计不好")
  需要手工 `exp add`。

[未发布]: https://github.com/asdfvbz/agent_exp/compare/v0.1.0...HEAD
[0.1.0]: https://github.com/asdfvbz/agent_exp/releases/tag/v0.1.0
